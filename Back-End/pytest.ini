[pytest]
# Rode a partir de Back-End: `python -m pytest`. pythonpath=. deixa o pacote
# `edubot` importável sem instalar. Os testes rodam em SQLite na memória
# (ver tests/conftest.py) — não tocam o MySQL.
pythonpath = .
testpaths = tests
